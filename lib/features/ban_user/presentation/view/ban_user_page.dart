import 'package:flutter/material.dart';

class BanUserPage extends StatelessWidget {
  const BanUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BanUserPage'),
      ),
    );
  }
}

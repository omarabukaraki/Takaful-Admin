import 'package:flutter/material.dart';

class GenerateAnaliticesPage extends StatelessWidget {
  const GenerateAnaliticesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GenerateAnaliticesPage'),
      ),
    );
  }
}
